import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
const root=path.resolve('out');
const base='https://viasos.it';
const municipalities=JSON.parse(fs.readFileSync('data/local-seo/municipalities.json','utf8'));
const contacts=JSON.parse(fs.readFileSync('data/local-seo/contacts.json','utf8'));
const nameCounts=new Map();
for(const c of municipalities)nameCounts.set(c.name,(nameCounts.get(c.name)||0)+1);
export const slug=s=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
export function distance(a,b){const rad=Math.PI/180;const dlat=(b.lat-a.lat)*rad,dlon=(b.lng-a.lng)*rad;return 6371*2*Math.asin(Math.sqrt(Math.min(1,Math.sin(dlat/2)**2+Math.cos(a.lat*rad)*Math.cos(b.lat*rad)*Math.sin(dlon/2)**2)))}
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const json=s=>JSON.stringify(s).replace(/</g,'\\u003c');
const fmt=n=>Math.round(n).toLocaleString('it-IT');
const regionUrl=c=>`/carroattrezzi/${slug(c.region)}/`;
const provinceUrl=c=>`${regionUrl(c)}${slug(c.province)}/`;
const cityUrl=c=>`${provinceUrl(c)}${slug(c.name)}/`;
const pages=[],mapping=[];
const write=(url,content)=>{const file=path.join(root,url,'index.html');if(fs.existsSync(file)) throw Error(`Collisione pagina ${url}`);fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,content);pages.push({url,hash:crypto.createHash('sha256').update(content).digest('hex')})};
const phoneIcon='<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M7 3H4a1 1 0 0 0-1 1c0 9.4 7.6 17 17 17a1 1 0 0 0 1-1v-3l-5-2-2 2a15 15 0 0 1-7-7l2-2-2-5Z"/></svg>';
const call=(p,label='Chiama ora',className='call')=>`<a class="${className}" href="tel:${p.tel}" data-call="${esc(p.city)}">${phoneIcon}<span>${label}<small>${esc(p.phone)}</small></span><span aria-hidden="true">↗</span></a>`;
const footer=`<footer class="footer wrap"><a class="brand" href="/"><img src="/images/viasos-logo-header-cropped.webp" alt="ViaSOS" width="728" height="241"></a><p>ViaSOS mette in contatto gli automobilisti con professionisti indipendenti. Disponibilità, copertura, prezzo e tempi sono confermati dal professionista. P. IVA 02606820690.</p><nav aria-label="Informazioni"><a href="/carroattrezzi/">Tutti i comuni</a><a href="/privacy/">Privacy</a><a href="/termini/">Termini</a><a href="/carroattrezzi/fonti/">Fonti e metodo</a></nav></footer>`;
function document({title,description,url,body,crumbs=[],p}){
const schema=[{'@context':'https://schema.org','@type':'WebPage','@id':base+url,name:title,url:base+url,description,inLanguage:'it-IT',publisher:{'@type':'Organization',name:'ViaSOS',url:base}},{'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{name:'ViaSOS',url:'/'},...crumbs].map((c,i)=>({'@type':'ListItem',position:i+1,name:c.name,item:base+c.url}))}];
return `<!doctype html><html lang="it"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><meta name="robots" content="index,follow,max-snippet:-1"><link rel="canonical" href="${base+url}"><meta property="og:type" content="website"><meta property="og:locale" content="it_IT"><meta property="og:site_name" content="ViaSOS"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${base+url}"><meta name="twitter:card" content="summary"><meta name="theme-color" content="#081b25"><link rel="stylesheet" href="/local-assets/local.css?v=2"><link rel="stylesheet" href="/local-assets/roadside.css?v=2"><script defer src="/local-assets/local.js?v=2"></script><script type="application/ld+json">${json(schema)}</script></head><body${p?' class="has-call"':''}><a class="skip" href="#main">Vai al contenuto</a><header class="top"><div class="wrap top-inner"><a class="brand" href="/" aria-label="ViaSOS, homepage"><img src="/images/viasos-logo-header-cropped.webp" alt="ViaSOS" width="728" height="241"></a><a class="directory" href="/carroattrezzi/">Trova il tuo comune <span>↗</span></a>${p?`<a class="header-call" href="tel:${p.tel}" aria-label="Chiama ${esc(p.phone)}">${phoneIcon}<span>${esc(p.phone)}</span></a>`:''}</div></header><main id="main">${crumbs.length?`<nav class="crumbs wrap" aria-label="Percorso"><a href="/">Home</a>${crumbs.map((c,i)=>`<span aria-hidden="true">/</span>${i===crumbs.length-1?`<span aria-current="page">${esc(c.name)}</span>`:`<a href="${c.url}">${esc(c.name)}</a>`}`).join('')}</nav>`:''}${body}</main>${footer}${p?`<aside class="sticky-call" aria-label="Contatto rapido"><span>Auto ferma?<small>Verifica subito l’intervento</small></span>${call(p)}</aside>`:''}</body></html>`;
}
function localArticle(c,p,nearby,ranked){
const caps=c.cap.length?c.cap.map(esc).join(', '):'da confermare con la posizione esatta';
const far=ranked[0].km>90;
const island=['Sardegna','Sicilia'].includes(c.region);
return `<section class="section wrap reading" id="informazioni"><div><p class="eyebrow">Prima di chiamare</p><h2>Soccorso stradale a ${esc(c.name)}.<br><em>Le informazioni che contano.</em></h2><p class="intro">Posizione precisa, mezzo adatto e un prezzo concordato: una richiesta chiara aiuta a organizzare il recupero.</p><nav class="article-nav" aria-label="In questa pagina"><a href="#zona">01 · Zona e contatto</a><a href="#costo">02 · Costi e preventivo</a><a href="#veicolo">03 · Tipo di veicolo</a><a href="#domande">04 · Domande frequenti</a></nav></div><article class="prose">
<h2 id="zona">Carroattrezzi a ${esc(c.name)}: quale numero chiamare</h2>
<p>Se cerchi un carroattrezzi a ${esc(c.name)}, in ${esc(c.region)}, puoi partire dal numero <a href="tel:${p.tel}">${esc(p.phone)}</a>. Il contatto è associato a ${esc(p.city)} ed è quello geograficamente più vicino al centro del comune fra i numeri presenti in questo elenco ViaSOS. La selezione confronta le coordinate delle città: non indica la posizione in tempo reale di un mezzo, né garantisce che un operatore sia già libero. Durante la telefonata chiedi conferma della copertura della tua posizione e della possibilità di intervenire sul tuo veicolo.</p>
<h3>Come indicare la posizione nel territorio di ${esc(c.province)}</h3>
<p>Comunica il nome ${esc(c.name)}, la sigla provinciale ${esc(c.provinceCode)} e l’indirizzo o il punto preciso in cui si trova il mezzo. I CAP riportati nell’archivio per questo comune sono: ${caps}. Il codice postale può aiutare a distinguere località con nomi simili, ma non sostituisce un indirizzo. Se il veicolo si trova fuori dal centro, specifica il nome della frazione o della strada, un riferimento riconoscibile e l’eventuale direzione di marcia. Se puoi condividere la posizione dal telefono, chiedi al professionista quale canale utilizzare.</p>
<p>${far?`Il riferimento di ${esc(p.city)} dista circa ${fmt(ranked[0].km)} km in linea d’aria dal centro di ${esc(c.name)}: è un contatto distante, quindi la copertura va verificata prima di organizzare il soccorso. Non considerarlo un mezzo già nelle vicinanze.`:`Fra i riferimenti disponibili, ${esc(p.city)} si trova a circa ${fmt(ranked[0].km)} km in linea d’aria dal centro di ${esc(c.name)}. Il percorso stradale può essere più lungo e dipende dal luogo effettivo di partenza del carroattrezzi.`} ${island?'Per un intervento su un’isola verifica esplicitamente che il professionista possa raggiungere la località: un confronto geografico non tiene conto di traghetti, collegamenti marittimi e accessibilità.':'Il traffico, gli accessi e la compatibilità dell’attrezzatura incidono sulla possibilità di effettuare il recupero.'} Per confrontare una località vicina, puoi consultare ${nearby.slice(0,3).map(n=>`<a href="${cityUrl(n)}">${esc(n.name)}</a>`).join(', ')}.</p>
<h2 id="costo">Soccorso stradale economico: chiedi il costo completo</h2>
<p>Per trovare una soluzione economica a ${esc(c.name)}, descrivi prima il problema e chiedi un preventivo comprensibile. Il prezzo di un carroattrezzi può includere uscita, caricamento, chilometri di trasporto e operazioni aggiuntive. Comunica sia il punto di ritiro sia la destinazione desiderata: un’officina scelta da te, una carrozzeria o un altro indirizzo concordato. Chiedi se l’importo comprende IVA, eventuali maggiorazioni legate all’orario e costi di deposito. La pagina non pubblica una tariffa fissa perché condizioni e prezzo vengono stabiliti dal professionista per la richiesta concreta.</p>
<h3>Valutare l’intervento adatto, prima del traino</h3>
<p>Un mancato avviamento, una gomma danneggiata e un guasto meccanico possono richiedere soluzioni differenti. Spiegare i sintomi permette al professionista di valutare se sia possibile un’assistenza sul posto o se serva il trasporto. Non dare per certo che ogni problema possa essere risolto senza traino. Se hai già una copertura di assistenza, consulta le condizioni o il relativo contatto prima di autorizzare una spesa: modalità di attivazione e destinazioni ammesse dipendono dal contratto. Confronta preventivi riferiti allo stesso servizio, non soltanto un prezzo iniziale.</p>
<h2 id="veicolo">Recupero auto, moto, furgone o camper</h2>
<p>Quando richiedi soccorso stradale a ${esc(c.name)}, indica marca, modello e tipo di veicolo. Per un furgone o un camper servono anche dimensioni e peso indicativi; per una moto chiarisci se può essere spostata e se ci sono danni che complicano il fissaggio. Segnala ruote bloccate, cambio non sbloccabile, sospensioni danneggiate o spazi di manovra ridotti. Se il mezzo è elettrico o ibrido, precisalo subito, così il professionista può valutare attrezzatura e modalità di trasporto compatibili con le indicazioni del costruttore.</p>
<h3>Parcheggi, autorimesse e accessi difficili</h3>
<p>Un recupero in garage può dipendere dall’altezza del soffitto, dalla larghezza della rampa e dalla possibilità di raggiungere il veicolo con l’attrezzatura. In un cortile o in un parcheggio privato, indica eventuali cancelli e restrizioni di accesso. Sul territorio di ${esc(c.name)} la sola posizione del centro comunale non descrive queste condizioni: riferisci sempre il punto esatto del fermo. Prima di confermare, chiedi come verrà caricato il mezzo e quali operazioni sono comprese nel preventivo ricevuto.</p>
<h2>Disponibilità e tempi: conferma durante la chiamata</h2>
<p>Il pulsante Chiama ora apre direttamente il numero selezionato sul tuo dispositivo. L’orario visibile è l’ora corrente italiana e non rappresenta un aggiornamento della flotta. Chiedi se c’è un carroattrezzi disponibile per la tua posizione, quale sia il tempo indicativo e come ricevere aggiornamenti se cambia. Se il primo contatto non può intervenire, in questa pagina trovi altri riferimenti ordinati per distanza geografica. ViaSOS facilita il contatto; il professionista conferma disponibilità, prezzo e svolgimento dell’intervento.</p>
</article></section>`;
}
const regions=new Map(),provinces=new Map();
for(const c of municipalities){if(!regions.has(c.region))regions.set(c.region,[]);regions.get(c.region).push(c);if(!provinces.has(provinceUrl(c)))provinces.set(provinceUrl(c),[]);provinces.get(provinceUrl(c)).push(c)}
for(const c of municipalities){
const ranked=contacts.map(p=>({...p,km:distance(c.coordinates,p.coordinates)})).sort((a,b)=>a.km-b.km||a.city.localeCompare(b.city));
const p=ranked[0],url=cityUrl(c);
const near=provinces.get(provinceUrl(c)).filter(n=>n.id!==c.id).map(n=>({...n,km:distance(c.coordinates,n.coordinates)})).sort((a,b)=>a.km-b.km).slice(0,6);
const title=`Carroattrezzi ${c.name}${nameCounts.get(c.name)>1?` (${c.provinceCode})`:''} - Soccorso stradale economico`;
const description=`Cerchi un carroattrezzi a ${c.name} (${c.provinceCode})? Chiama ${p.phone}: contatto di ${p.city}. Verifica disponibilità, tempi e preventivo per il soccorso stradale.`;
const faqs=[
[`C’è un carroattrezzi disponibile ora a ${c.name}?`,`Chiama ${p.phone} per verificarlo. L’orario sul sito è l’ora corrente italiana: disponibilità e tempi di arrivo vengono confermati dal professionista, non dall’orologio della pagina.`],
[`Quanto costa un carroattrezzi a ${c.name}?`,'Il prezzo dipende da recupero, distanza, veicolo e condizioni di accesso. Chiedi un preventivo completo con IVA, trasporto, eventuale deposito e maggiorazioni prima di confermare.'],
[`Perché vedo il numero di ${p.city}?`,`È la città più vicina al centro di ${c.name} fra i contatti presenti nell’elenco. Il calcolo usa distanze in linea d’aria e non la posizione attuale dei mezzi. La copertura della tua località va confermata per telefono.`],
['Posso scegliere l’officina di destinazione?','Comunica l’indirizzo desiderato prima di concordare il recupero. Il professionista verifica fattibilità, distanza e costo; se utilizzi una copertura di assistenza, controlla le condizioni previste.']];
const body=`<section class="hero wrap"><div class="hero-copy"><p class="eyebrow"><span class="dot"></span> ${esc(c.region)} <span class="muted">/ ${esc(c.provinceCode)}</span></p><h1>Carroattrezzi<br><em>${esc(c.name)}.</em></h1><p class="hero-sub">Soccorso stradale vicino a te.<br><strong>Chiama. Concorda il prezzo. Riparti.</strong></p><div class="now"><span class="dot"></span><div><strong>Carroattrezzi disponibile ora</strong><span>Chiama per confermare disponibilità e tempi</span><span class="clock" data-clock></span></div></div>${call(p)}<p class="micro">Contatto di ${esc(p.city)} · ${p.km>90?'Copertura della località da verificare':'Verifica tempi e copertura al telefono'}</p><a class="text-link" href="#costo">Cerchi un soccorso stradale economico? ↓</a></div><div class="radar" aria-label="Illustrazione del soccorso stradale"><div class="radar-grid"></div><div class="ring ring-one"></div><div class="ring ring-two"></div><div class="ring ring-three"></div><div class="radar-sweep"></div><svg class="tow-art" viewBox="0 0 600 300" fill="none" aria-hidden="true"><path class="road-lines" d="M0 260H600M20 280H100M150 280H230M280 280H360M410 280H490M540 280H600" stroke="currentColor" stroke-width="3"/><path d="M50 213H366V187H417L420 116H477L531 172V231H508M50 213V232H99M147 232H416M464 232H460" fill="#17243b" stroke="#ffb51b" stroke-width="5" stroke-linejoin="round"/><path d="M435 128H470L510 170H434V128Z" fill="#6dc8ed" fill-opacity=".35" stroke="#ffb51b" stroke-width="4"/><path d="M55 205H379L348 188H78L55 205Z" fill="#ffb51b"/><path d="M119 173L136 135L180 108H250L289 139H323L341 170V186H119V173Z" fill="#dbe7ed" stroke="#dbe7ed" stroke-width="4"/><path d="M158 135L185 117H241L263 139H157Z" fill="#14243a"/><circle cx="156" cy="184" r="19" fill="#101923" stroke="#dbe7ed" stroke-width="5"/><circle cx="300" cy="184" r="19" fill="#101923" stroke="#dbe7ed" stroke-width="5"/><circle cx="124" cy="230" r="27" fill="#101923" stroke="#ffb51b" stroke-width="6"/><circle cx="441" cy="230" r="27" fill="#101923" stroke="#ffb51b" stroke-width="6"/><circle cx="124" cy="230" r="10" fill="#8ea0b0"/><circle cx="441" cy="230" r="10" fill="#8ea0b0"/><path d="M416 108H460" stroke="#fb8b17" stroke-width="10" stroke-linecap="round" class="beacon"/><path d="M438 81V68M410 86L401 77M464 86L473 77" stroke="#ffb51b" stroke-width="4" stroke-linecap="round" class="beacon-rays"/><path d="M77 221H100M176 221H225M245 221H294M315 221H364" stroke="#ffb51b" stroke-width="6"/><path d="M516 193H530" stroke="#fff4c8" stroke-width="7"/></svg><div class="radar-center"><span>IL TUO COMUNE</span><strong>${esc(c.name)}</strong><small>${esc(c.provinceCode)} · ${esc(c.region)}</small></div><div class="radar-tag"><span class="dot"></span> Riferimento: ${esc(p.city)}</div><p class="radar-note">Soccorso e recupero veicoli<br>Disponibilità e tempi da confermare</p></div></section>
<section class="benefits wrap" aria-label="Come richiedere assistenza"><div><span>01</span><h2>Chiama direttamente</h2><p>Il numero è pronto. Nessun modulo obbligatorio.</p></div><div><span>02</span><h2>Descrivi il fermo</h2><p>Posizione, veicolo e problema: parti da qui.</p></div><div><span>03</span><h2>Concorda il costo</h2><p>Chiedi disponibilità e preventivo prima di confermare.</p></div></section>
<section class="section wrap" id="contatti"><div class="section-heading"><div><p class="eyebrow">Contatti e territorio</p><h2>Il riferimento più vicino.<br><em>La scelta, al telefono.</em></h2></div><p>Ordine basato sui centri delle città nell’elenco ViaSOS. Le distanze non sono chilometri stradali né tempi di arrivo.</p></div><div class="contact-grid">${ranked.slice(0,3).map((r,i)=>`<article class="contact-card ${i===0?'featured':''}"><span class="eyebrow">${i===0?'Primo riferimento':'Alternativa '+i}</span><h3>${esc(r.city)}</h3><p>Circa ${fmt(r.km)} km in linea d’aria</p>${call(r,i===0?'Chiama ora':'Chiama il contatto')}<small>Copertura e disponibilità da confermare</small></article>`).join('')}</div></section>
${localArticle(c,p,near,ranked)}
<section class="quote-band"><div class="wrap"><p class="eyebrow">Il prossimo passo è semplice</p><h2>Auto ferma a ${esc(c.name)}?<br><em>Parliamone, ora.</em></h2><p>Chiedi la soluzione adatta e il costo dell’intervento.</p>${call(p)}</div></section>
<section class="section wrap faq" id="domande"><div><p class="eyebrow">Risposte utili</p><h2>Prima di<br><em>ripartire.</em></h2></div><div>${faqs.map(([q,a])=>`<details><summary>${esc(q)}<span aria-hidden="true">+</span></summary><p>${esc(a)}</p></details>`).join('')}</div></section>
<section class="section wrap nearby"><p class="eyebrow">Esplora il territorio</p><h2>Nei dintorni di ${esc(c.name)}</h2><div class="link-grid">${near.map(n=>`<a href="${cityUrl(n)}"><span>Carroattrezzi ${esc(n.name)}</span><span>↗</span></a>`).join('')}</div><a class="text-link" href="${provinceUrl(c)}">Tutti i comuni: ${esc(c.province)} →</a></section>`;
write(url,document({title,description,url,body,p,crumbs:[{name:'Carroattrezzi',url:'/carroattrezzi/'},{name:c.region,url:regionUrl(c)},{name:c.province,url:provinceUrl(c)},{name:c.name,url}]}));
mapping.push({id:c.id,name:c.name,region:c.region,url,city:p.city,tel:p.tel,distanceKm:+p.km.toFixed(2),alternatives:ranked.slice(1,3).map(r=>({city:r.city,tel:r.tel}))});
}
const cards=list=>`<div class="link-grid">${list.map(([name,url,n])=>`<a href="${url}"><span>${esc(name)}${n?`<small>${n} comuni</small>`:''}</span><span>↗</span></a>`).join('')}</div>`;
for(const [url,list] of provinces){const c=list[0];write(url,document({title:`Carroattrezzi in provincia di ${c.province} | ViaSOS`,description:`Trova il contatto per il soccorso stradale nei ${list.length} comuni del territorio di ${c.province}. Telefoni e informazioni per richiedere un preventivo.`,url,crumbs:[{name:'Carroattrezzi',url:'/carroattrezzi/'},{name:c.region,url:regionUrl(c)},{name:c.province,url}],body:`<section class="section wrap"><p class="eyebrow">${esc(c.region)} / ${list.length} comuni</p><h1>Carroattrezzi<br><em>${esc(c.province)}.</em></h1><p class="intro">Seleziona il comune in cui si trova il veicolo. Troverai il riferimento telefonico più vicino fra quelli presenti nell’elenco, con le informazioni per verificare copertura e costo.</p>${cards(list.sort((a,b)=>a.name.localeCompare(b.name,'it')).map(c=>[c.name,cityUrl(c)]))}</section>`}));}
for(const [region,list] of regions){const c=list[0],url=regionUrl(c);const grouped=[...provinces].filter(([,l])=>l[0].region===region);write(url,document({title:`Carroattrezzi ${region}: comuni e contatti | ViaSOS`,description:`Soccorso stradale in ${region}: scegli la provincia e il comune, trova il riferimento telefonico e chiedi disponibilità e preventivo.`,url,crumbs:[{name:'Carroattrezzi',url:'/carroattrezzi/'},{name:region,url}],body:`<section class="section wrap"><p class="eyebrow">${list.length} comuni / ${grouped.length} territori provinciali</p><h1>Carroattrezzi<br><em>${esc(region)}.</em></h1><p class="intro">Parti dal territorio. Scegli la provincia per trovare il contatto da chiamare e le informazioni utili al recupero del veicolo.</p>${cards(grouped.map(([u,l])=>[l[0].province,u,l.length]))}</section>`}));}
write('/carroattrezzi/',document({title:'Carroattrezzi vicino a te: cerca il tuo comune | ViaSOS',description:'Trova il contatto per il soccorso stradale nel tuo comune. Numeri ordinati per vicinanza geografica, chiamata diretta e informazioni per il preventivo.',url:'/carroattrezzi/',crumbs:[{name:'Carroattrezzi',url:'/carroattrezzi/'}],body:`<section class="section wrap"><p class="eyebrow">ViaSOS / Comuni d’Italia</p><h1>Il tuo comune.<br><em>La prossima chiamata.</em></h1><p class="intro">Cerca dove si è fermato il veicolo e trova il riferimento più vicino fra i nostri ${contacts.length} contatti. Verifica direttamente disponibilità e preventivo.</p><div class="search"><label for="city-search">In quale comune ti trovi?</label><input id="city-search" type="search" placeholder="Es. Bergamo, Abano Terme, Udine…" autocomplete="off" aria-describedby="search-help"><p id="search-help">Scrivi almeno 2 caratteri. Puoi cercare anche per provincia.</p><p data-search-status role="status"></p><div id="search-results" class="link-grid"></div></div><h2>Oppure esplora per regione</h2>${cards([...regions].sort(([a],[b])=>a.localeCompare(b,'it')).map(([r,l])=>[r,regionUrl(l[0]),l.length]))}</section>`}));
write('/carroattrezzi/fonti/',document({title:'Fonti geografiche e selezione dei contatti | ViaSOS',description:'Origine dei dati comunali e metodo di associazione dei numeri telefonici ViaSOS. Limiti delle distanze geografiche e verifica della disponibilità.',url:'/carroattrezzi/fonti/',crumbs:[{name:'Carroattrezzi',url:'/carroattrezzi/'},{name:'Fonti e metodo',url:'/carroattrezzi/fonti/'}],body:`<section class="section wrap prose"><h1>Fonti e metodo.</h1><h2>Dati dei comuni</h2><p>L’archivio utilizzato comprende ${municipalities.length} comuni. Fonte: <a href="https://github.com/RP92/comuni-italiani">RP92/comuni-italiani</a>, dati derivati da ISTAT e Garda Informatica, licenza <a href="https://creativecommons.org/licenses/by/4.0/">CC BY 4.0</a>. Coordinate mancanti integrate da <a href="https://github.com/opendatasicilia/comuni-italiani">OpenDataSicilia</a>. Archivio acquisito il 12 settembre 2026. Sono stati selezionati denominazione, territorio, CAP e coordinate; l’archivio non è una pubblicazione ufficiale ViaSOS o ISTAT.</p><h2>Selezione del numero</h2><p>Il calcolo confronta le coordinate del centro comunale con quelle delle città associate ai numeri pubblicati sui siti della rete. Il primo numero è quello con distanza sferica minore in linea d’aria. Non utilizziamo il prefisso telefonico per dedurre la posizione. I numeri privi di un contatto telefonico valido sono esclusi. Le distanze non considerano traffico, traghetti o la posizione dei singoli mezzi.</p><h2>Disponibilità e prezzi</h2><p>L’orario mostrato è quello corrente nel fuso Europe/Rome, non un aggiornamento operativo. Copertura, disponibilità, preventivi e tempi vanno confermati dal professionista contattato. ViaSOS è un intermediario, non una sede di soccorso presente in ogni comune. La ricerca di un soccorso economico richiede un preventivo per il caso concreto: non è pubblicata una tariffa minima garantita.</p><h2>Riferimenti utilizzati</h2>${cards(contacts.map(p=>[`${p.city} · ${p.phone}`,p.site]))}<p>Per segnalazioni sui dati: <a href="mailto:info@viasos.it">info@viasos.it</a>.</p></section>`}));
fs.mkdirSync(path.join(root,'local-assets'),{recursive:true});
for(const file of ['local.css','roadside.css','local.js'])fs.copyFileSync(`scripts/local-seo/${file}`,path.join(root,'local-assets',file));
fs.writeFileSync(path.join(root,'local-assets/cities.json'),json(municipalities.map(c=>({name:c.name,province:c.province,code:c.provinceCode,url:cityUrl(c)}))));
// Preserve existing URLs but replace build-time timestamps on local pages with content-based dates.
const existing=fs.readFileSync(path.join(root,'sitemap.xml'),'utf8');
fs.writeFileSync(path.join(root,'sitemap-core.xml'),existing);
const previous=fs.existsSync('data/local-seo/page-manifest.json')?JSON.parse(fs.readFileSync('data/local-seo/page-manifest.json','utf8')):{};
const today=new Date().toISOString().slice(0,10),manifest={};
for(const p of pages)manifest[p.url]={hash:p.hash,lastmod:previous[p.url]?.hash===p.hash?previous[p.url].lastmod:today};
fs.writeFileSync('data/local-seo/page-manifest.json',JSON.stringify(manifest));
const xml=urls=>`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(u=>`<url><loc>${base+u}</loc><lastmod>${manifest[u].lastmod}</lastmod></url>`).join('')}</urlset>`;
const maps=['sitemap-core.xml'];
for(const [region,list] of regions){const name=`sitemap-${slug(region)}.xml`;maps.push(name);fs.writeFileSync(path.join(root,name),xml(list.map(cityUrl)));}
maps.push('sitemap-territori.xml');fs.writeFileSync(path.join(root,'sitemap-territori.xml'),xml(pages.filter(p=>!mapping.some(c=>c.url===p.url)).map(p=>p.url)));
fs.writeFileSync(path.join(root,'sitemap.xml'),`<?xml version="1.0" encoding="UTF-8"?><sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${maps.map(m=>`<sitemap><loc>${base}/${m}</loc></sitemap>`).join('')}</sitemapindex>`);
fs.writeFileSync('data/local-seo/routing-audit.json',JSON.stringify(mapping));
console.log(`Local SEO: ${municipalities.length} comuni, ${provinces.size} province, ${regions.size} regioni, ${contacts.length} contatti, ${pages.length} pagine.`);
